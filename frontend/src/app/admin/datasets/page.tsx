export default function DatasetsAdminPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold">Dataset Management</h1>
      <p className="mt-2 text-sm text-muted">Upload and manage training datasets for each disease module.</p>
      <div className="mt-6 rounded-2xl border border-border bg-surface p-8 text-center text-sm text-muted">
        No data yet — connect the datasets admin service to populate this view.
      </div>
    </div>
  );
}
