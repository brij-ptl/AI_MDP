export default function ModelsAdminPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold">Model Training</h1>
      <p className="mt-2 text-sm text-muted">Trigger retraining jobs and review model version history.</p>
      <div className="mt-6 rounded-2xl border border-border bg-surface p-8 text-center text-sm text-muted">
        No data yet — connect the models admin service to populate this view.
      </div>
    </div>
  );
}
