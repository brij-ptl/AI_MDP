export default function AnalyticsAdminPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold">Prediction Analytics</h1>
      <p className="mt-2 text-sm text-muted">Aggregate accuracy, usage and revenue analytics across modules.</p>
      <div className="mt-6 rounded-2xl border border-border bg-surface p-8 text-center text-sm text-muted">
        No data yet — connect the analytics admin service to populate this view.
      </div>
    </div>
  );
}
